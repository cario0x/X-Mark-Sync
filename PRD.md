# X-Mark Sync - Product Requirements Document (PRD)

## 1. 产品概述
X-Mark Sync 是一款桌面端自动化工具，旨在帮助用户管理和备份 Twitter (X) 书签。它通过模拟真实浏览器行为登录 X，定期抓取书签内容，并将其转换为 Obsidian 可读的 Markdown 笔记。

## 2. 用户流程
1.  **初始化**：用户首次运行程序，程序弹出一个浏览器窗口，用户手动登录 Twitter 账号。登录状态（Cookies）将被保存。
2.  **同步配置**：用户设置本地存储路径（Obsidian 仓库路径）和同步频率。
3.  **抓取过程**：
    - 程序在后台（或前台可视）打开书签页面。
    - 识别书签分类（如果有，如 X Premium 文件夹；如果没有，默认归档到 "Uncategorized" 或按时间归档）。
    - 抓取推文详情：正文、图片/视频链接、发布时间、原推链接。
4.  **存储过程**：
    - 将每条推文（或每天的书签集合）保存为 Markdown 文件。
    - 下载媒体文件到本地附件文件夹（可选，MVP阶段可仅保留远程链接）。

## 3. 核心功能需求 (MVP)

### 3.1 账户授权 (Auth)
- 使用 Playwright 启动带有 `user_data_dir` 的浏览器上下文。
- 检测是否已登录，未登录则等待用户交互。

### 3.2 书签抓取 (Scraper)
- 访问 `https://x.com/i/bookmarks`。
- 自动向下滚动加载更多。
- **增量更新机制**：记录上次同步到的最后一条推文 ID，避免重复抓取。

### 3.3 内容解析 (Parser)
- 提取推文文本。
- 提取媒体（图片 `img src`, 视频缩略图）。
- 提取元数据（作者, 时间戳）。

### 3.4 输出模块 (Exporter)
- 生成标准 Markdown。
- 文件命名规范：`{YYYY-MM-DD}-{TweetID}.md` 或按日期汇总 `Bookmarks-{YYYY-MM-DD}.md`。
- 目录结构：
  ```
  Obsidian_Vault/
    └── Twitter Bookmarks/
        ├── Tech/
        │   └── 2023-10-27-AI-News.md
        └── Art/
            └── 2023-10-26-Design-Tips.md
  ```

## 4. 非功能需求
- **稳定性**：处理网络波动和页面加载超时。
- **安全性**：所有 Cookies 和数据仅存储在本地，不上传任何服务器。
- **易用性**：提供简单的配置文件 (`config.yaml`) 或 CLI 交互。

## 5. 风险与对策
- **反爬虫/风控**：使用 Playwright 模拟真实用户操作（随机等待、鼠标移动），避免高频 API 调用。
- **页面结构变更**：X 前端更新频繁，CSS 选择器需独立配置，方便维护更新。

